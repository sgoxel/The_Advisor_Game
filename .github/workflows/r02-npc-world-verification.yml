name: R02 NPC World Verification

on:
  push:
    branches: [main]
    paths:
      - 'js/npc_world.js'
      - 'js/utils.js'
      - 'tests/browser/r02-npc-world-presence.spec.mjs'
      - '.github/workflows/r02-npc-world-verification.yml'
  pull_request:
    paths:
      - 'js/npc_world.js'
      - 'js/utils.js'
      - 'tests/browser/r02-npc-world-presence.spec.mjs'
      - '.github/workflows/r02-npc-world-verification.yml'

permissions:
  contents: read

jobs:
  npc-world-and-regression:
    runs-on: ubuntu-latest
    timeout-minutes: 18
    steps:
      - name: Checkout exact repository state
        uses: actions/checkout@v4
      - name: Record exact candidate SHA
        run: git rev-parse HEAD
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install pinned browser test dependency
        run: npm install --ignore-scripts
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run local NPC world-presence and routine regression
        run: npx playwright test tests/browser/r02-npc-world-presence.spec.mjs
      - name: Re-run deterministic origin village regression
        run: npx playwright test tests/browser/r02-origin-village.spec.mjs
      - name: Re-run deterministic multi-region terrain regression
        run: npx playwright test tests/browser/r02-multi-region-terrain.spec.mjs
      - name: Run stabilized R01 browser regression
        run: npm run test:browser:r01
      - name: Upload Playwright evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: r02-t16-npc-world-evidence-${{ github.sha }}
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
          retention-days: 14
