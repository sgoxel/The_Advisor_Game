name: R04 Starter Village Exteriors Verification

on:
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'js/config.js'
      - 'js/spatial_world.js'
      - 'js/starter_village_exteriors.js'
      - 'js/starter_village_dev_overlay.js'
      - 'js/protagonist_routine.js'
      - 'js/utils.js'
      - 'tests/browser/r04-starter-village-exteriors.spec.mjs'
      - 'tests/browser/r04-village-dev-protagonist-routine.spec.mjs'
      - 'playwright.config.mjs'
      - 'package.json'
      - '.github/workflows/r04-starter-village-exteriors-verification.yml'
  pull_request:
    paths:
      - 'index.html'
      - 'js/config.js'
      - 'js/spatial_world.js'
      - 'js/starter_village_exteriors.js'
      - 'js/starter_village_dev_overlay.js'
      - 'js/protagonist_routine.js'
      - 'js/utils.js'
      - 'tests/browser/r04-starter-village-exteriors.spec.mjs'
      - 'tests/browser/r04-village-dev-protagonist-routine.spec.mjs'
      - 'playwright.config.mjs'
      - 'package.json'
      - '.github/workflows/r04-starter-village-exteriors-verification.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  starter-village-exteriors:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout exact repository state
        uses: actions/checkout@v4

      - name: Record exact candidate SHA
        run: git rev-parse HEAD

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install pinned browser test dependency
        run: npm install --ignore-scripts

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run focused starter-village exterior regression
        run: npx playwright test tests/browser/r04-starter-village-exteriors.spec.mjs tests/browser/r04-village-dev-protagonist-routine.spec.mjs

      - name: Upload Playwright evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: r04-starter-village-exteriors-evidence-${{ github.sha }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 14
