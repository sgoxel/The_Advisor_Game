name: R02 Multi-Region Terrain Verification

on:
  push:
    branches: [main]
    paths:
      - 'js/rng.js'
      - 'tests/browser/r02-multi-region-terrain.spec.mjs'
      - '.github/workflows/r02-multi-region-terrain-verification.yml'
  pull_request:
    paths:
      - 'js/rng.js'
      - 'tests/browser/r02-multi-region-terrain.spec.mjs'
      - '.github/workflows/r02-multi-region-terrain-verification.yml'

permissions:
  contents: read

jobs:
  multi-region-terrain-and-regression:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout exact repository state
        uses: actions/checkout@v4
      - name: Record exact candidate SHA
        run: git rev-parse HEAD
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install pinned browser test dependency
        run: npm install --ignore-scripts
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run focused multi-region terrain regression
        run: npx playwright test tests/browser/r02-multi-region-terrain.spec.mjs --project=desktop
      - name: Re-run world-coordinate and deterministic foundations
        run: npx playwright test tests/browser/r02-world-coordinate-contract.spec.mjs tests/browser/r02-seed-rng.spec.mjs --project=desktop
      - name: Run stabilized R01 browser regression
        run: npm run test:browser:r01
      - name: Upload Playwright evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: r02-t14-multi-region-evidence-${{ github.sha }}
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
          retention-days: 14
