name: Migrate recent unverified issue closures

on:
  push:
    paths:
      - .github/workflows/migrate_recent_unverified_closures.yml
  workflow_dispatch: {}

permissions:
  contents: read
  issues: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Bootstrap
        run: echo migration-bootstrap-ok
