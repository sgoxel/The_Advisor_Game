name: R04 NPC Residency Verification

on:
  push:
    branches: [main]
    paths:
      - 'js/npc_residency.js'
      - 'js/utils.js'
      - 'js/spatial_world.js'
      - 'js/game_time.js'
      - 'js/campaign_persistence.js'
      - 'tests/browser/r04-npc-residency-lifecycle.spec.mjs'
      - '.github/workflows/r04-npc-residency-verification.yml'
  pull_request:
    paths:
      - 'js/npc_residency.js'
      - 'js/utils.js'
      - 'js/spatial_world.js'
      - 'js/game_time.js'
      - 'js/campaign_persistence.js'
      - 'tests/browser/r04-npc-residency-lifecycle.spec.mjs'
      - '.github/workflows/r04-npc-residency-verification.yml'

permissions:
  contents: read

jobs:
  npc-residency-lifecycle:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout exact repository state
        uses: actions/checkout@v4
      - name: Record exact candidate SHA
        run: git rev-parse HEAD
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install pinned browser test dependency
        run: npm install --ignore-scripts
      - name: Install Chromium
        run: npx playwright install --with-deps chromium
      - name: Run focused NPC residency lifecycle regression
        run: npx playwright test tests/browser/r04-npc-residency-lifecycle.spec.mjs
      - name: Upload Playwright evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: r04-npc-residency-evidence-${{ github.sha }}
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
          retention-days: 14
