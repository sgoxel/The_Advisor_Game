# Binary staging fallback

GitHub is authoritative. When a required Admin-provided binary asset already exists but the active GitHub connector cannot directly publish its bytes, follow `WORKFLOW.md` for the authorized repository-sync staging fallback. Staging is not completion; verify the final files in GitHub before handoff to Tester.
