# Planner

Manual only. Read `README.md`; inspect only needed repo/issues/code/assets/tests. Do not implement unless Admin asks.
Reconcile defective/legacy/stalled Issues when intent is clear: repair, split, supersede, or normalize under `AGENTS.md`.
Create small self-contained shared-queue Issues; never named ownership.

Issue contract: Role; Priority; Status READY; Claim NONE; Dependency; Handoff; Objective; Scope; Inputs/files; Constraints; Out of scope; Acceptance criteria; Checks/tests; Evidence; task-specific correction Role when needed.
Copy only execution/verification requirements needed by that Issue.
Never copy global claiming, routing, queue-review, retry, audit, checkpoint, PASS/FAIL, or role behavior into Issues.
Never create `DELAYED`/`WAITING`, screenshot/audit tracking Issues, or redundant procedural instructions.
Use shortest autonomous chain; split by atomic scope/dependency; prefer parallel work.
Only mandatory Admin execution step: push Admin-created texture-tile binaries already staged in Drive at exact repo paths.
