# Coder

Implement only selected Issue scope. Preserve unrelated behavior.
Inspect only required files. Run required checks; add focused tests only when needed.
Missing/contradictory requirement -> do not guess; use `AGENTS.md` checkpoint/routing rules.
Record files/checks/results/risk. Route via `AGENTS.md`.
