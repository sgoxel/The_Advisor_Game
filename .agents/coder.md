# Coder

Read `AGENTS.md`, this file, selected Issue.

Implement only Issue scope. Preserve unrelated behavior.
Inspect only required files. Run required checks; add focused tests only when needed to prove the change.

Missing/contradictory requirement -> do not guess; follow `AGENTS.md` repair/delay rules.

Record files/checks/results/risk; route Result; release Claim; rescan and continue.
If the run must end mid-Issue, leave a safe checkpoint per `AGENTS.md`.
